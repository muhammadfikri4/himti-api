// import { Router } from "express";
// import { upload } from "./alumniConfig";
// import { createAlumniController, deleteAlumniController, getAlumniController, updateAlumniController } from "./alumniController";

// const route = Router()

// route.post("/", upload.single("image"), createAlumniController)
// route.get("/", getAlumniController)
// route.delete("/:id", deleteAlumniController)
// route.put("/:id", upload.single("image"), updateAlumniController)

// export default route